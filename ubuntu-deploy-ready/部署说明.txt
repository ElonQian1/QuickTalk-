# Ubuntu HTTPS 部署说明
生成时间: 2025-10-17 15:25:06

## 文件清单

### 后端程序
- customer-service-backend (10.71 MB) - 最新编译，支持 HTTPS

### 证书文件
- certs/server.crt - SSL证书
- certs/server.key - 私钥

### 前端静态文件
- static/ (11个文件, 2.22 MB)

### 配置文件
- .env - 基础配置
- .env.production - 生产环境配置（推荐）
- .env.staging - 测试环境配置

### 服务配置
- customer-service.service - systemd 服务文件

## 部署步骤

1. 上传整个文件夹到服务器: /root/ubuntu-deploy-ready/

2. 设置执行权限:
   chmod +x /root/ubuntu-deploy-ready/customer-service-backend

3. 使用生产配置启动:
   cd /root/ubuntu-deploy-ready
   cp .env.production .env
   ./customer-service-backend

4. 或配置为系统服务:
   cp customer-service.service /etc/systemd/system/
   systemctl daemon-reload
   systemctl enable customer-service
   systemctl start customer-service

## HTTPS 访问

服务启动后访问: https://elontalk.duckdns.org:8443

## 注意事项

- 后端编译时间: 2025-10-17 15:24
- 支持 ACME 自动证书续期
- 使用 Sea-ORM 自动数据库迁移
- 零系统依赖（静态编译）

