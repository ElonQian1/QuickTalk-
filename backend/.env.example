# 客服系统环境配置示例
# 复制此文件为 .env 并根据需要修改配置

# 基础数据库配置
DATABASE_URL=sqlite:customer_service.db

# JWT密钥 (生产环境请使用复杂的随机字符串)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 服务器基础配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# ==========================================
# HTTPS配置 (可选)
# ==========================================

# 启用HTTPS (true/false)
HTTPS_ENABLED=false

# HTTPS端口
HTTPS_PORT=8443

# SSL证书文件路径
TLS_CERT_PATH=certs/server.crt
TLS_KEY_PATH=certs/server.key

# 证书域名 (用于证书生成)
TLS_DOMAIN=localhost

# HTTP到HTTPS重定向 (true/false)
REDIRECT_HTTP=false

# ==========================================
# 开发环境配置
# ==========================================

# 日志级别 (trace, debug, info, warn, error)
RUST_LOG=info

# 开发模式
DEV_MODE=true

# ==========================================
# 生产环境配置示例
# ==========================================

# 取消注释以下配置用于生产环境:

# HTTPS_ENABLED=true
# HTTPS_PORT=443
# TLS_CERT_PATH=/etc/ssl/certs/your-domain.crt
# TLS_KEY_PATH=/etc/ssl/private/your-domain.key
# TLS_DOMAIN=yourdomain.com
# REDIRECT_HTTP=true
# SERVER_PORT=80
# RUST_LOG=warn

# ==========================================
# 使用说明
# ==========================================

# 1. HTTP模式 (默认):
#    - 设置 HTTPS_ENABLED=false 或不设置
#    - 运行: cargo run

# 2. HTTPS模式:
#    - 设置 HTTPS_ENABLED=true
#    - 配置证书路径
#    - 生成证书: ./generate-cert.bat (开发环境)
#    - 运行: cargo run --features https

# 3. 同时运行HTTP和HTTPS:
#    - 设置 HTTPS_ENABLED=true
#    - 设置 REDIRECT_HTTP=true
#    - HTTP会自动重定向到HTTPS