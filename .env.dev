# 开发环境配置 - 智能协议模式
# 优先尝试 HTTPS，失败时自动回退到 HTTP

# 基本配置
DATABASE_URL=sqlite:customer_service.db
JWT_SECRET=dev-secret-key-for-testing-only
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# 智能协议配置
TLS_MODE=auto
TLS_PORT=8443
TLS_CERT_PATH=certs/server.crt
TLS_KEY_PATH=certs/server.key
TLS_DOMAIN=localhost
TLS_REDIRECT_HTTP=true

# 开发模式设置
RUST_LOG=debug
CORS_ALLOWED_ORIGINS=https://localhost:8443,https://127.0.0.1:8443,http://localhost:8080,http://127.0.0.1:8080

# 智能模式说明
# TLS_MODE 可选值:
#   auto/smart - 智能模式：优先HTTPS，失败时回退HTTP (推荐)
#   https/true/force - 强制HTTPS模式，失败时退出
#   http/false/disabled - 强制HTTP模式
# 
# 访问地址: 
#   优先: https://localhost:8443 (如果证书可用)
#   备用: http://localhost:8080 (如果HTTPS失败)
