# 客服系统 HTTPS 配置示例
# 复制此文件为 .env 并根据需要修改配置

# === 基础服务器配置 ===
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# === 数据库配置 ===
DATABASE_URL=sqlite:customer_service.db
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# === HTTPS/TLS 配置 ===
# 启用HTTPS (true/false)
TLS_ENABLED=false

# HTTPS端口
TLS_PORT=8443

# SSL证书文件路径
TLS_CERT_PATH=cert.pem
TLS_KEY_PATH=key.pem

# 域名 (用于自签名证书生成)
TLS_DOMAIN=localhost

# 启用HTTP到HTTPS重定向 (true/false)
TLS_REDIRECT_HTTP=true

# 自动生成证书 (开发环境) (true/false)
TLS_AUTO_GENERATE=true

# === Let's Encrypt ACME 配置 (生产环境) ===
# 域名列表 (逗号分隔)
ACME_DOMAINS=example.com,www.example.com

# ACME证书文件路径
ACME_CERT_PATH=acme_cert.pem
ACME_KEY_PATH=acme_key.pem

# 联系邮箱 (必填)
ACME_EMAIL=admin@example.com

# 使用生产环境 (true/false，false为测试环境)
ACME_PRODUCTION=false

# HTTP验证端口 (通常为80)
ACME_HTTP_PORT=80

# === 开发环境示例 ===
# 要启用HTTPS，请设置:
# TLS_ENABLED=true
# TLS_CERT_PATH=./cert.pem  
# TLS_KEY_PATH=./key.pem
# TLS_PORT=8443

# === 生产环境示例 ===
# TLS_ENABLED=true
# TLS_CERT_PATH=/etc/ssl/certs/yourdomain.crt
# TLS_KEY_PATH=/etc/ssl/private/yourdomain.key
# TLS_PORT=443
# TLS_DOMAIN=yourdomain.com