# 开发环境 HTTPS 配置文件
# 用于解决Mixed Content问题，在localhost开发环境启用HTTPS
# 使用方法: 将此文件重命名为 .env 然后启动应用

# 基本配置
DATABASE_URL=sqlite:customer_service.db
JWT_SECRET=dev-super-secret-jwt-key-2024
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# HTTPS 配置 - 启用开发环境HTTPS
TLS_ENABLED=true
TLS_PORT=8443
TLS_CERT_PATH=certs/dev-server.crt
TLS_KEY_PATH=certs/server.key
TLS_DOMAIN=localhost
TLS_REDIRECT_HTTP=false

# 开发配置
RUST_LOG=debug